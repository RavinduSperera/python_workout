# math_functions
import math

pi  = 3.14
x = 3
y = 4
z = 8

print(round(pi)) # rounding to the nearest number
print(math.ceil(pi)) # roundig to the upper number
print(math.floor(pi)) # rounding to the lower number
print(abs(pi)) # absalute values (negetives make postives)
print(pow(pi, 2)) # 2 to the power (2 can change)
print(math.sqrt(pi)) # squre root fucntions
print(max(x, y, z)) # largest number
print(min(x, y, z)) # smallest number